apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: rl-arena
data:
  # Server Configuration
  PORT: "8080"
  ENV: "production"
  
  # CORS Configuration
  CORS_ALLOWED_ORIGINS: "http://localhost:3000,http://localhost:5173"
  
  # Matchmaking Configuration
  MATCHMAKING_INTERVAL: "10s"
  MAX_ELO_DIFFERENCE: "200"
  
  # Executor Service (gRPC with K8s Service DNS)
  EXECUTOR_GRPC_URL: "rl-arena-executor.rl-arena.svc.cluster.local:50051"
  
  # K8s Configuration
  K8S_NAMESPACE: "rl-arena"
  USE_K8S: "true"
  
  # Container Registry (변경 필요)
  CONTAINER_REGISTRY: "docker.io/your-registry"
  
  # Logging
  LOG_LEVEL: "info"
  
  # Timezone
  TZ: "Asia/Seoul"
